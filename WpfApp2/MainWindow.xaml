<Window x:Class="WpfApp2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Server" Height="450" Width="800">

    <Grid Margin="20">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Server hệ thống điểm danh"
                   FontSize="24"
                   FontWeight="Bold"
                   Grid.ColumnSpan="2"/>

        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,15,0,0">
            <TextBlock x:Name="txtIp" FontSize="16"/>
            <TextBlock x:Name="txtStatus" FontSize="14" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- QR Code -->
        <Border Grid.Row="1"
                Grid.Column="1"
                BorderThickness="1"
                BorderBrush="Gray"
                Padding="10"
                HorizontalAlignment="Center">

            <Image x:Name="qrImage"
                   Width="200"
                   Height="200"/>
        </Border>

        <!-- Danh sách sinh viên -->
        <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,20,10,0">

            <TextBlock Text="Sinh viên vừa điểm danh:"
                       FontWeight="Bold"
                       FontSize="14"
                       Margin="0,0,0,5"/>

            <ListBox x:Name="lstStudents"
                     Height="200"/>
        </StackPanel>

        <!-- Log server -->
        <TextBox Grid.Row="2"
                 Grid.Column="1"
                 x:Name="txtLog"
                 Margin="10,20,0,0"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 IsReadOnly="True"/>

    </Grid>
</Window>